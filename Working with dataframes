--We are using the "diamonds" dataset that is a part of ggplot2 package, so we have to install it first.

install.packages("tidyverse")
library(ggplot2)
data("diamonds")
View(diamonds)

--to view only top six rows, 

head(diamonds)
str(diamonds)
colnames(diamonds)

--mutate --To make changes to dataframe 
--load tidyverse library

library(tidyverse)
mutate(diamonds, carat_2 = carat * 100)
